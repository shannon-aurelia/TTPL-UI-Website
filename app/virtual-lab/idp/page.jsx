'use client';
import {useState} from 'react';
export default function IDPSim(){const [mode,setMode]=useState('V'),[v,setV]=useState(12),[r,setR]=useState(1000); const val=mode==='V'?v:mode==='A'?v/r:r; return <section className="section"><h1 className="title">IDP Instrument Trainer.</h1><div className="sim-grid"><div className="meter"><div className="meter-screen">{mode==='A'?(val*1000).toFixed(2)+' mA':mode==='V'?val.toFixed(2)+' V':val.toFixed(0)+' Ω'}</div><div className="knob"/><p style={{fontWeight:900}}>Multimeter mode</p><button className="chip" onClick={()=>setMode('V')}>Voltage</button><button className="chip" onClick={()=>setMode('A')}>Current</button><button className="chip" onClick={()=>setMode('R')}>Resistance</button></div><div className="card"><h2>Training note</h2><p>Voltage is measured in parallel. Current is measured in series. Resistance is measured on a disconnected component.</p><label>Source {v} V<input className="slider" type="range" min="1" max="24" value={v} onChange={e=>setV(+e.target.value)}/></label><label>Resistance {r} Ω<input className="slider" type="range" min="100" max="10000" step="100" value={r} onChange={e=>setR(+e.target.value)}/></label></div></div></section>}
